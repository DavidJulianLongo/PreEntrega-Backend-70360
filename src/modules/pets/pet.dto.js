export default class PetDTO {

    constructor(pet) {

        this.name = pet.name;
        this.type = pet.type;
        this.birthDate = pet.birthDate;
        this.adopted = pet.adopted;
    }
}

